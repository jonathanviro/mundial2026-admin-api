services:
  db:
    image: postgres:17-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: MySecretPass
      POSTGRES_DB: mundial_2026_db_develop
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
