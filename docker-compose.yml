services:
  db:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: MySecretPass
      POSTGRES_DB: mundial_2026_db
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
